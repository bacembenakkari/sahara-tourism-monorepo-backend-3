spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/duneinsolite
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: duneinsolite
    password: duneinsolite123
    listener:
      simple:
        acknowledge-mode: manual
        retry:
          enabled: true
          initial-interval: 5000
          max-attempts: 3
          multiplier: 2

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/duneinsolite

  mail:
    host: smtp.gmail.com
    port: 587
    username: bacemakkari25@gmail.com
    password: wfze sgwp oxwl zmeh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false                      # set to true temporarily if emails aren't sending

keycloak:
  admin:
    server-url: http://localhost:8180
    realm: duneinsolite
    client-id: duneinsolite-api
    client-secret: DzXfwEHyTCynUJtKevpCXlz5yVtBBUYK
  realm: duneinsolite

server:
  port: 8080

# ── App-level properties (used in EmailService) ─────────────────
app:
  mail:
    from: noreply@duneinsolite.com        # display sender address in outgoing emails
  frontend:
    url: http://localhost:4200            # ← replace with your deployed frontend URL in production